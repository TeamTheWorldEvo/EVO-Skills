# 快速模式（一键默认配置）

**触发条件**：用户输入匹配快速模式触发词（"默认配置初始化"、"一键默认"、"快速初始化"、"quick init"、"init-project --default"）。

加速通道：所有配置采用推荐默认值，一张表汇总，一次确认。

## Q0：识别意图

若用户消息同时含快速触发词 + 项目名（如"默认配置初始化 my-app"），直接提取名称 → Q2。否则先问项目信息。

## Q1：项目信息（如尚未提供）

同 Step 1 — 询问项目名称 + 一句话描述。

**注意**：从模式选择门（用户选 A）进入时，项目信息已在 Step 1 收集完毕，跳过 Q1 直接到 Q2。

## Q2：展示默认配置表

> 🚀 **一键默认配置**
>
> | 配置项 | 默认值 | 对应 Step |
> |--------|--------|----------|
> | 行为规则 | 4 条 Karpathy 规则全部启用 | Step 2 |
> | 批判性审查 | ✅ 启用 | Step 3.1 |
> | 上下文地图更新 | ✅ 启用 | Step 3.2 |
> | 工作目录约束 | ✅ 启用 | Step 3.3 |
> | Bash 沙箱 | ✅ 开启 + 自动允许 | Step 4.1 |
> | 额外工作目录 | 空（仅限项目目录） | Step 4.2 |
> | Bash 白名单 | 预置清单 | Step 5.2 |
> | 文件访问黑名单 | 推荐配置（平台感知） | Step 5.3 |
> | MCP 与额外权限 | 全开 + WebSearch + Workflow(deep-research) | Step 5.4 |
> | Git 提交归属 | 不加 | Step 5.5 |
> | 记忆系统 | 本地记忆（`./memory/` + `./MEMORY.md`） | Step 6 |
> | .claude/ 结构 | 全建 | Step 7 |
>
> 输入 **ok** 确认并生成，或告诉我要改什么（如「不要上下文地图更新」「记忆用全局的」）。
>
> 若用户想了解某一项，引用对应 Step 的详细解释（从 references/ 加载），单项处理完毕后回到汇总表。

## Q3：生成与报告

用户确认后进入 Step 9（生成）→ Step 10（报告）。硬性约束依然适用。

✅ 完成条件：已确认配置项数 == 12（汇总表行数）；已生成文件数 == 5（CLAUDE.md + settings.json + settings.local.json + MEMORY.md + memory/）
